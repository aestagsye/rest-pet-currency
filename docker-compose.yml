services:
  postgres:
    image: postgres:18
    container_name: pet-currency-postgres
    environment:
      POSTGRES_DB: currency_exchange
      POSTGRES_USER: currency_user
      POSTGRES_PASSWORD: currency_password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: